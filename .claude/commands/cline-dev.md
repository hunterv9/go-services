Launch a cline-dev agent to delegate the following sub-task to Cline CLI. Verify cline is available first, then invoke with path-scoped instructions.

Task: $ARGUMENTS

Return a concise summary in Vietnamese with:
- Cline command run and exit code
- Changed files
- Result summary (pass/fail)
