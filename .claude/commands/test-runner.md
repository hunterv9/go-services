Launch a test-runner agent to write and/or execute tests for the following task. Use Bash to run test commands directly. Report exact errors on failure.

Task: $ARGUMENTS

Return a concise summary in Vietnamese with:
- Test command run and status (Passed/Failed/Skipped counts)
- Defect details if any (file:line + error + repro steps)
