Launch a UX/UI agent to design the following interface. Create HTML/CSS mockups directly using existing design tokens. Ensure responsive and accessible layouts.

Task: $ARGUMENTS

Return a concise summary in Vietnamese with:
- Mockup file path created
- Key UI components and tokens used
