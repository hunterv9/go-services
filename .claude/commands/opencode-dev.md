Launch an opencode-dev agent to delegate the following sub-task to OpenCode CLI. Verify opencode is available first, then invoke with path-scoped instructions.

Task: $ARGUMENTS

Return a concise summary in Vietnamese with:
- OpenCode command run and exit code
- Changed files
- Result summary (pass/fail)
