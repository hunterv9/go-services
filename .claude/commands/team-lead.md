Launch a team-lead agent to orchestrate the following task. The team-lead MUST delegate all work to specialist agents (dev, test, security, qc, ux-ui, pm, etc.) via the Agent tool — never do implementation work itself.

Task: $ARGUMENTS

Requirements:
- Classify task size and invoke ONLY needed specialists
- Each agent call must specify exact files/paths and expected output
- Return a status table in Vietnamese showing each agent's progress
